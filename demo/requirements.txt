# Python deps for the SingleRust × scverse demo notebook.
# Tested with Python 3.9. Install into a venv:
#   python3 -m venv .venv && . .venv/bin/activate && pip install -r demo/requirements.txt
scanpy>=1.10
anndata>=0.10
cellxgene-census>=1.15
hdf5plugin>=4.0          # required to read SingleRust's blosc/zstd-compressed .h5ad
psutil                  # peak-memory tracking in the 500k scaling notebook
jupyter
nbconvert
ipykernel
matplotlib
leidenalg
igraph
pandas
numpy
scipy
